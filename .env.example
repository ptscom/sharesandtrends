# Optional Upstox API access tokens (server-side fallback when browser keys are not set).
# Never commit real tokens. Copy to .env.local for local development.
UPSTOX_ACCESS_TOKEN_1=
UPSTOX_ACCESS_TOKEN_2=
UPSTOX_ACCESS_TOKEN_3=
UPSTOX_ACCESS_TOKEN_4=
